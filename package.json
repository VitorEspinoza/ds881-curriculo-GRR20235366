{
  "name": "ds881-curriculo-grr20235366",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "browser-sync start --server src --files \"src/**/*.html, src/assets/css/**/*.css, src/assets/js/**/*.js\" --port 8080 --no-notify --no-open",
    "lint:html": "htmlhint \"src/**/*.html\"",
    "lint:css": "stylelint \"src/assets/css/**/*.css\"",
    "lint": "npm run lint:html && npm run lint:css",
    "build": "node scripts/build.js"
  },
  "devDependencies": {
    "browser-sync": "^3.0.0",
    "htmlhint": "^1.1.4",
    "stylelint": "^16.0.0",
    "stylelint-config-standard": "^36.0.0",
    "html-minifier-terser": "^7.2.0",
    "clean-css": "^5.3.3"
  }
}
